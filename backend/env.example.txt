# ============================================================
#  Dhan Options Dashboard - backend configuration
#  COPY this file to a file named ".env" in this same folder
#  (backend/.env) and fill in your real values.
#  NEVER commit ".env" to git.
# ============================================================

# --- Dhan credentials (SECRET - backend only, never sent to browser) ---
DHAN_CLIENT_ID=1000000000
DHAN_ACCESS_TOKEN=eyJ...your-access-token-here
# TIP: set DHAN_ACCESS_TOKEN=mock-token to run fully offline with synthetic data
# (great for exploring the UI before you have a live token).

# --- App security ---
# Long random strings. Generate with:
#   python -c "import secrets;print(secrets.token_urlsafe(48))"
APP_SESSION_SECRET=change-me-to-a-long-random-string
# Order confirmation PIN (6-8 digits). Required server-side to place any order.
APP_ORDER_PIN=123456

# --- Safety switches (also flippable live from the Settings tab) ---
# true  -> orders are validated + logged but NOT sent to Dhan (SAFE, default)
# false -> orders are sent to Dhan for real
APP_DRY_RUN=true
# true  -> order PIN is SKIPPED: 1 click places the order (scalping / fast mode)
# false -> every order requires the PIN (safe, default)
APP_FAST_ORDERS=false

# --- Logging (everything saved for debugging) ---
# Folder for daily log files (relative to backend/, or absolute).
LOG_DIR=logs
# Files older than this many days are auto-deleted (startup + once a day).
LOG_RETENTION_DAYS=7
# DEBUG / INFO / WARNING / ERROR
LOG_LEVEL=INFO

# --- Network ---
APP_HOST=127.0.0.1
APP_PORT=8000
# Comma separated, explicit origins only. Never "*".
APP_ALLOWED_ORIGINS=http://127.0.0.1:5173,http://localhost:5173

# --- Option chain poller (FREE tier: 1 request / 3s per underlying) ---
# Comma separated underlying keys the poller keeps warm. See instruments.py.
OC_POLLED_INDICES=NIFTY,SENSEX
OC_POLL_INTERVAL_SECONDS=3

# --- Intelligence / news (optional) ---
# Custom market RSS feeds for the Market Pulse panel.
# Format: "Name|url;Name|url". Leave empty to use built-in Indian market feeds.
# APP_NEWS_FEEDS=
# Default look-back window (days) for the expired-options history study (7-180).
INTEL_HISTORY_DAYS=90

